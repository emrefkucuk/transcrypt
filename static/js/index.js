// index.js - Provides global functions for use in HTML onclick handlers
// This file is loaded as a regular script (not a module)

// Global function placeholders that will be populated by main.js
// These functions are called directly from HTML onclick attributes

// Initialize placeholder functions that will be overridden by the module system
window.showCreateRoomOptions = function() {
    // Will be replaced at runtime
};

window.showJoinRoomOptions = function() {
    // Will be replaced at runtime
};

window.showP2POptions = function() {
    // Will be replaced at runtime
};

window.goBack = function() {
    // Will be replaced at runtime
};

window.selectKeyOption = function() {
    // Will be replaced at runtime
};

window.copyToClipboard = function() {
    // Will be replaced at runtime
};

window.createRoom = function() {
    // Will be replaced at runtime
};

window.joinRoomWithKey = function() {
    // Will be replaced at runtime
};

window.extractKeyAndJoin = function() {
    // Will be replaced at runtime
};

window.extractKeyFromImageAndJoin = function() {
    // Will be replaced at runtime
};

window.continueAsSender = function() {
    // Will be replaced at runtime
};

window.downloadStegoImage = function() {
    // Will be replaced at runtime
};

window.leaveRoom = function() {
    // Will be replaced at runtime
};

window.sendFile = function() {
    // Will be replaced at runtime
};

window.downloadFile = function() {
    // Will be replaced at runtime
};

window.regenerateStegoText = function() {
    // Will be replaced at runtime
};

window.validateConnectionCode = function() {
    // Will be replaced at runtime
};

window.toggleTheme = function() {
    // Will be replaced at runtime
};

window.selectP2PRole = function() {
    // Will be replaced at runtime
};

window.startCamera = function() {
    // Will be replaced at runtime
};

window.capturePhoto = function() {
    // Will be replaced at runtime
};

window.verifyFaceForRooms = function() {
    // Will be replaced at runtime
};